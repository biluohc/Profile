#cat ~/.cargo/config 
#http://doc.crates.io/config.html

[source.crates-io]
registry = "git://github.com/rust-lang/crates.io-index"

#ustc炸了只需注释下面一行,cargo-vendor可以把依赖下载到本地。
#replace-with = 'ustc'
[source.ustc]
registry = "git://mirrors.ustc.edu.cn/crates.io-index"

[http]
#proxy = "127.0.0.1:1080"       # HTTP proxy to use for HTTP requests (defaults to none)
timeout = 60000     # Timeout for each HTTP request, in milliseconds
#cainfo = "cert.pem" # Path to Certificate Authority (CA) bundle (optional)

[build]
target-dir = "/home/viw/.cache/mozilla/cargo"     #指定编译目标目录,remdisk failed tp complian win32sys(windows)

[alias]
n = "new --bin"
new = "new"
b = "build"
br =  "build  --release"
t = "test"
r  =  "run"
rr = "run --release"
d = "doc"
